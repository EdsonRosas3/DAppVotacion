import React from 'react';
import { Outlet } from 'react-router-dom';

const Organization = () => {
  return (
    <div>
      Organization
      <Outlet/>
    </div>

  )
}

export default Organization